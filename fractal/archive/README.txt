# TODO: smart contract accepts swap requests
    DONE
# TODO: swap request can be "revoked" after a time frame given by swap requester
    DONE
# TODO: swap requests can include payment combination of ERC20 token and native token
    DONE
# TODO: smart contract accepts simulated block submissions
# TODO: smart contract distributes rewards to simulated block submitter and swap request fulfiller
# TODO: simulated block submissions can "rewrite" existing blocks
  if simulated block submission is a chain longer than the current chain and "rewrites" some simulated blocks
  "revoke" rewards from all "rewritten" blocks
# TODO: reduce block submission rewards overtime and increase difficult (cost) of block submission, similar to bitcoin
